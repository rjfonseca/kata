# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

tasks:
  test:
    internal: true
    silent: true
    set:
      - pipefail
    cmds:
      - mkdir -p .kata
      - PYTHONPATH=src python3 -m doctest -v src/*_test.py 2>&1 | tee .kata/last_run.log
