# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

tasks:
  test:
    internal: true
    silent: true
    set:
      - pipefail
    cmds:
      - mkdir -p .kata
      - go test ./... 2>&1 | tee .kata/last_run.log
