# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

tasks:
  test:
    silent: true
    internal: true
    set:
      - pipefail
    cmds:
      - mkdir -p .kata
      - |
        docker compose exec -T hurl \
          hurl --test  --error-format long --pretty . \
          2>&1 | tee .kata/last_run.log
