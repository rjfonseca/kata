# Step 5: Complex Expressions

Handle more complex expressions with multiple operators and also negative numbers.

**Example Inputs:**
- `"15 7 1 1 + - / 3 * 2 1 1 + + -"` -> `4`
- `"-5"` -> `-5`
- `"1 -5 +"` -> `-4`
